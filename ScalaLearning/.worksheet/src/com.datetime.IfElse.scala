package com.datetime

import java.util.Calendar

import java.text.SimpleDateFormat

object IfElse {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(131); 

val x = if (a > b) a else b;System.out.println("""x  : <error> = """ + $show(x ))}

}
